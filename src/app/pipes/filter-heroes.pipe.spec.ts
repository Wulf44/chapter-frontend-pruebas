import { FilterMHeroesPipe } from './filter-heroes.pipe';

describe('FilterMHeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMHeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
